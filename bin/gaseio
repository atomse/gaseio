#!/bin/bash


filename=$1

if [ ! -e $filename ]; then
    echo $filename not exist
    exit 1
fi




python -c "import json_tricks, gaseio; x = gaseio.read(\"$filename\"); print(json_tricks.dumps(x))"
